var fctbtns = document.querySelectorAll('.fct_btn')
var tcards = document.querySelectorAll('.card_style_t')

for (let i = 0; i < fctbtns.length; i++) {
    const element = fctbtns[i];
    const element = tcards[i];
    console.log();
       
}